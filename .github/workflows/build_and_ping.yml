name: Build and Ping
on:  
  workflow_dispatch:  

jobs:
  build_and_ping:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v2
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v0
        with:
          project_id: github-wiki-see
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true
      - name: Cache Processed Results
        id: cache-results
        uses: actions/cache@v3
        with:
          path: generator/cache
          key: ccache-${{ github.run_id }}
          restore-keys: |
            ccache-
      - name: Set up Python
        id: setup-python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - uses: actions/cache@v3
        with:
          path: .venv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}-${{ steps.setup-python.outputs.python-version }}
      - run: uv sync
      - run: uv run python generate.py --hours_back=400
        working-directory: ./generator
        env:
          PYTHONUNBUFFERED: 1
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          force_orphan: true
#       - uses: atymic/sitemap-ping-action@1.1.0
#         with:
#           sitemap-url: https://github-wiki-see.page/generated_sitemap.xml
